{
  "name": "lji-source-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "prestart": "npm test",
    "start": "node src/server.js",
    "test": "node --test"
  }
}
