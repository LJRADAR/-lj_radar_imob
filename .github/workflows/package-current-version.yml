name: Package current version

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - 'services/**'
      - 'supabase/**'
      - 'render.yaml'
      - '.github/workflows/package-current-version.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - 'services/**'
      - 'supabase/**'
      - 'render.yaml'
      - '.github/workflows/package-current-version.yml'

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build ZIP
        run: |
          zip -r LJ_Radar_Imob_v30.0_Clean.zip frontend services supabase render.yaml README.md -x '*.git*'
      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: LJ_Radar_Imob_v30.0_Clean
          path: LJ_Radar_Imob_v30.0_Clean.zip
          retention-days: 30
